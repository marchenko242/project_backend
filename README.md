<!-- REST API User Posts
This is a REST API project for managing user posts. It is built using Node.js, Express, MongoDB (via Mongoose), and TypeScript. The API includes features like user authentication, password hashing, and Swagger documentation.

🚀 Getting Started
Prerequisites
Before you begin, ensure you have the following installed:

Node.js (v18 or higher recommended)

npm (usually comes with Node.js)

MongoDB (either locally or a cloud instance like MongoDB Atlas)

🛠️ Technologies Used
Node.js: Runtime environment.

Express: Web framework.

Mongoose: MongoDB object modeling.

TypeScript: Static typing for JavaScript.

JWT: JSON Web Tokens for authentication.

Bcrypt: Password hashing.

Swagger UI: API documentation.

Nodemon: Development server with live reload.

ts-node: TypeScript execution environment.



Install dependencies:

npm install

Create a .env file in the root directory and add the following variables: -->

<!-- PORTHOST=3000
MONGO_URI="mongodb+srv://<db_username>:<db_password>@cluster0.7f78z.mongodb.net/"
KEYACCES=<your_acces_key>
KEYREFRESH=<your_acces_key> -->

<!-- To start the project, you need to click on the button next to the term "start:dev" and then you will have a successful start of the project on host 3000, but please check whether you have loaded the data from package.json and whether you have substituted the data for the database with keys and codes. -->